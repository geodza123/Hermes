name: replicate
version: 1.0.0
description: "Replicate generation — best-in-class image (FLUX 1.1 Pro Ultra) and video (Google Veo 3) models. Creates a prediction, waits for the result, downloads the file."
author: Georgiy (via Octeday)
kind: standalone
provides_tools:
  - replicate_image
  - replicate_video
requires_env:
  - REPLICATE_API_TOKEN
